<p>Title: <%= card.title%></p>
<p>Description: <%= card.description%></p>
<p class="button"><%=link_to "New todo", new_item_path(card_id: card.id)%></p>
<p class="button"><%=link_to "Move Card", move_card_path(card_id: card.id)%></p>
<table>
    <thead>
        <th>Item</th>
        <th>Private/Public</th>
        <th>Mark As Done</th>
    </thead>
    <tbody>
            <div class = "items">
<%@items= card.items%>
<%= render @items%>
            </div>
    </tbody>
</table>
