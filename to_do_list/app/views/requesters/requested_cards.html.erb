<% requesters.each do |requester| %>
<%= requester.card.title %>
<% @user=User.find(requester.user_id)%>
<%=@user.name%>
<span class="labe"><%= link_to "Accept", accept_request_path(request_id:requester.id)%>
    <span class="label"><%= link_to "reject", reject_request_path(request_id:requester.id)%>
    <% end %>

